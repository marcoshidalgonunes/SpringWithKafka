spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_accounts
    driver-class-name: org.postgresql.Driver
  kafka:
    bootstrap-servers: localhost:9092

feign:
  client:
    balance:
      url: http://localhost:8081/api/balance
